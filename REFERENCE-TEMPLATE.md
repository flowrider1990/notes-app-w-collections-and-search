# [Page title from the docs]

**Source:** https://
**Retrieved:** YYYY-MM-DD

---

<!--
Paste the actual documentation text below this line.

This file exists so the agent works from real documentation rather than from a training
cutoff. Content reconstructed from memory defeats its entire purpose and fails the criterion,
which specifies a cited reference and "not a placeholder."

Pages worth capturing, in rough priority order:

  1. Supabase — server-side rendering / Next.js App Router setup
     (the createClient patterns for browser vs server, and the session middleware)
  2. Supabase — Row Level Security policies
  3. supabase-js — the JavaScript client API for select / insert / update / delete
  4. supabase-js — querying related tables via foreign keys, for the note_tags join
  5. Next.js — dynamic route segments, since the params shape has changed between versions

Name the saved files after the topic, e.g. docs/supabase-ssr-nextjs.md.
One page is the minimum; two or three makes the agent noticeably less likely to guess.
-->
